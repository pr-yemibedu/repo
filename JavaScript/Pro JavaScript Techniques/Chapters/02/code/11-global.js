// A globally-scope variable, containing the string 'test'
var test = "test";

// You'll notice that our 'global' variable and the
// property of the window object are identical
alert( window.test == test );

