This example demonstrates the use of the FABridge which is a Flash/JavaScript integration component.

1. FABridge_Example.mxml get compiled into FABridge_Example.swf 
2. Run FABridge_Example.html from a web server

*** NOTE ***
FABridge_Example.html MUST be served up from a web server to work.  Can't be served up from the file system (no file://* path)